import { Colors } from '../theme/AmoLightTheme';
import { SxMuiStyle } from '../types/MUI.types';

export const settingsBackgroundStyle: SxMuiStyle = {
	display: 'flex',
	backgroundColor: Colors.white,
	flexDirection: 'column',
	p: '26px',
	gap: 2,
	boxShadow: 0,
};
