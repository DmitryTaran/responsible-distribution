import { Colors } from 'shared/theme/AmoLightTheme';
import { SxMuiStyle } from 'shared/types/MUI.types';

export const TableLayoutStyle: SxMuiStyle = {
	borderTop: `1px solid ${Colors.tableBorder}`,
	borderLeft: `1px solid ${Colors.tableBorder}`,
	borderRight: `1px solid ${Colors.tableBorder}`,
	borderRadius: '5px 5px 0px 0px',
}